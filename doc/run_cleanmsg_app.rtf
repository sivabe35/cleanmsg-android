{\rtf1\ansi\ansicpg1252\cocoartf2822
\cocoatextscaling0\cocoaplatform0{\fonttbl\f0\fswiss\fcharset0 Helvetica;}
{\colortbl;\red255\green255\blue255;}
{\*\expandedcolortbl;;}
\paperw11900\paperh16840\margl1440\margr1440\vieww11520\viewh8400\viewkind0
\pard\tx720\tx1440\tx2160\tx2880\tx3600\tx4320\tx5040\tx5760\tx6480\tx7200\tx7920\tx8640\pardirnatural\partightenfactor0

\f0\fs24 \cf0 export ANDROID_HOME=/Users/sivashankar.venkatas/Library/Android/sdk\
\
# 1. Build the debug APK\
./gradlew :app:assembleDebug\
\
# 2. Start the emulator (run in background, or just open AVD Manager in Android Studio)\
"$ANDROID_HOME/emulator/emulator" -avd Copy_of_Medium_Phone_API_34 &\
\
# 3. Wait for it to finish booting\
"$ANDROID_HOME/platform-tools/adb" wait-for-device\
until [ "$("$ANDROID_HOME/platform-tools/adb" shell getprop sys.boot_completed | tr -d '\\r')" = "1" ]; do sleep 3; done\
\
# 4. Install the APK\
"$ANDROID_HOME/platform-tools/adb" install -r app/build/outputs/apk/debug/app-debug.apk\
\
# 5. Launch the app\
"$ANDROID_HOME/platform-tools/adb" shell am start -n com.siva.cleanmsg.app/.MainActivity\
\
Note: this only works right now if you run it from a terminal using your Homebrew JDK 17 (that's what ./gradlew picked up when I ran it) \'97 it's a separate JVM from Android Studio's own "Gradle JVM" setting, which is why the IDE showed the JDK 25 error while the command line worked fine.}